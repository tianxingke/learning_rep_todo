一、
	一般来说，oracle数据库安装后，里面有很多个默认账号和密码，比较常用的是：账号：scott 密码:tiger   账号:system 密码:manager  账号:sys  密码:任意字符。
	第一次登陆的时候可以使用system 密码如果安装时设置了就使用安装时的密码。
	当我们想创建一个账号时，可以使用sys登录sysdba后，创建用户（需要授权），代码如下

	create user 用户名 identified by "密码";  
	授权：grant create session to 用户名;  
		    grant create table to  用户名;  
		    grant create tablespace to  用户名;  
		    grant create view to  用户名;
	我们一般使用的是用scott登录sysdba，这时候有可能出现账户未解锁的状态，这时候可以用

	alter user scott account unlock;
	来解锁账户。解锁之后可能会要求你改密码：可以用

	alter user scott identified by tiger;
	
	再登录
	conn scott/tiger;
	
	1.连接字符串：ip：port/服务名。
	2.每天学习和优化一个top sql。
	3.oracle 通信：  
		方式一：
		tnsping  ora10g
		sqlplus sys/oracle@10g  as  sysdba    走网络的方式
		ora10g    必须在 $ORACLE_HOME/network/admin/tnsnames.ora中进行配置。
		方式二：
		sqlplus sys/oracle@IP地址:1521/ora10g  as  sysdba
		不需要任何配置  ，但oracle服务器必须在listener.ora定义被远程的方式。
		
		oracle网络配置：
		
	4.连接
	sqlpus / as sysdba   这种方式不走网络
	conn   /    as    sysdba

	alter user  scott  identified  by   tiger   account    unlock;


	conn   scott/tiger











二、常用语句

	1.语法 : next_day( date, weekday ) 
		next_day，next_day(x,y)用于计算x时间后第一个星期y的时间。x 是一个时间，y 是星期一至星期日中的一个。也可能用数字1 - 7代替，但是用数字的时候1是代表的周日2才是代表的周一

		使用方法并不复杂，主要注意如下：
		1）  当第二个参数传的星期数比现有星期数小的时候，会返回下一个星期的日期；当第二个参数所传的星期数比 现有的星期数大的时候，则会返回本周的相应星期日期。

		
		可以求出当前日期的下个星期一。
		select next_day( sysdate, 'MONDAY') from dual;
		

	2.TRUNC函数为指定元素而截去的日期值。
	
	其具体的语法格式如下：
	　　TRUNC（date[,fmt]）
	　　其中：
	　　date 一个日期值
	　　fmt 日期格式，该日期将由指定的元素格式所截去。忽略它则由最近的日期截去

	3.oracle查看表下的触发器：
		select t.TRIGGER_NAME,t.STATUS, t.* from all_triggers t where t.TABLE_NAME='T_BDGSUB' and t.OWNER='IFMIS' ; --与用plsql单独到表下去看是一样的。


		select * from user_views;
		select *from user_tables;
	

	4.oracle对象和表的解锁和锁定
	
	5.oracle删除用户下的所有表的数据，保留表
		begin
			for x in (select table_name from user_tables) loop
				execute immediate 'delete ' || x.table_name ; 
			end loop ; 
		end ;
	
	6.sql进行格式化
		xx 333
		xxxxx 44
		xx 555
		xxxxx 66    
		这种格式使用union 可以实现

		

		123456 xxx oo
		123456 xxx oo
		这种格式使用left join 可以实现
	7.修改表字段名
		alter table A rename column a to c















